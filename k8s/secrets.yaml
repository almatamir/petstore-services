# DO NOT commit real values here.
# Create this file locally with your actual keys, or inject via CI/CD secrets.
apiVersion: v1
kind: Secret
metadata:
  name: petstore-secrets
  namespace: petstore
type: Opaque
stringData:
  NINJA_API_KEY: "replace-with-real-key"
  OWNER_SECRET: "replace-with-real-secret"
